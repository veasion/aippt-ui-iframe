var _ = Object.defineProperty;
var N = (e, r, t) => r in e ? _(e, r, { enumerable: !0, configurable: !0, writable: !0, value: t }) : e[r] = t;
var d = (e, r, t) => N(e, typeof r != "symbol" ? r + "" : r, t);
const I = "%[a-f0-9]{2}", b = new RegExp("(" + I + ")|([^%]+?)", "gi"), w = new RegExp("(" + I + ")+", "gi");
function h(e, r) {
  try {
    return [decodeURIComponent(e.join(""))];
  } catch {
  }
  if (e.length === 1)
    return e;
  r = r || 1;
  const t = e.slice(0, r), n = e.slice(r);
  return Array.prototype.concat.call([], h(t), h(n));
}
function q(e) {
  try {
    return decodeURIComponent(e);
  } catch {
    let r = e.match(b) || [];
    for (let t = 1; t < r.length; t++)
      e = h(r, t).join(""), r = e.match(b) || [];
    return e;
  }
}
function k(e) {
  const r = {
    "%FE%FF": "��",
    "%FF%FE": "��"
  };
  let t = w.exec(e);
  for (; t; ) {
    try {
      r[t[0]] = decodeURIComponent(t[0]);
    } catch {
      const i = q(t[0]);
      i !== t[0] && (r[t[0]] = i);
    }
    t = w.exec(e);
  }
  r["%C2"] = "�";
  const n = Object.keys(r);
  for (const i of n)
    e = e.replace(new RegExp(i, "g"), r[i]);
  return e;
}
function R(e) {
  if (typeof e != "string")
    throw new TypeError("Expected `encodedURI` to be of type `string`, got `" + typeof e + "`");
  try {
    return decodeURIComponent(e);
  } catch {
    return k(e);
  }
}
function E(e, r) {
  if (!(typeof e == "string" && typeof r == "string"))
    throw new TypeError("Expected the arguments to be of type `string`");
  if (e === "" || r === "")
    return [];
  const t = e.indexOf(r);
  return t === -1 ? [] : [
    e.slice(0, t),
    e.slice(t + r.length)
  ];
}
function D(e, r) {
  const t = {};
  if (Array.isArray(r))
    for (const n of r) {
      const i = Object.getOwnPropertyDescriptor(e, n);
      i != null && i.enumerable && Object.defineProperty(t, n, i);
    }
  else
    for (const n of Reflect.ownKeys(e)) {
      const i = Object.getOwnPropertyDescriptor(e, n);
      if (i.enumerable) {
        const a = e[n];
        r(n, a, e) && Object.defineProperty(t, n, i);
      }
    }
  return t;
}
const H = (e) => e == null, L = (e) => encodeURIComponent(e).replaceAll(/[!'()*]/g, (r) => `%${r.charCodeAt(0).toString(16).toUpperCase()}`), g = Symbol("encodeFragmentIdentifier");
function P(e) {
  switch (e.arrayFormat) {
    case "index":
      return (r) => (t, n) => {
        const i = t.length;
        return n === void 0 || e.skipNull && n === null || e.skipEmptyString && n === "" ? t : n === null ? [
          ...t,
          [c(r, e), "[", i, "]"].join("")
        ] : [
          ...t,
          [c(r, e), "[", c(i, e), "]=", c(n, e)].join("")
        ];
      };
    case "bracket":
      return (r) => (t, n) => n === void 0 || e.skipNull && n === null || e.skipEmptyString && n === "" ? t : n === null ? [
        ...t,
        [c(r, e), "[]"].join("")
      ] : [
        ...t,
        [c(r, e), "[]=", c(n, e)].join("")
      ];
    case "colon-list-separator":
      return (r) => (t, n) => n === void 0 || e.skipNull && n === null || e.skipEmptyString && n === "" ? t : n === null ? [
        ...t,
        [c(r, e), ":list="].join("")
      ] : [
        ...t,
        [c(r, e), ":list=", c(n, e)].join("")
      ];
    case "comma":
    case "separator":
    case "bracket-separator": {
      const r = e.arrayFormat === "bracket-separator" ? "[]=" : "=";
      return (t) => (n, i) => i === void 0 || e.skipNull && i === null || e.skipEmptyString && i === "" ? n : (i = i === null ? "" : i, n.length === 0 ? [[c(t, e), r, c(i, e)].join("")] : [[n, c(i, e)].join(e.arrayFormatSeparator)]);
    }
    default:
      return (r) => (t, n) => n === void 0 || e.skipNull && n === null || e.skipEmptyString && n === "" ? t : n === null ? [
        ...t,
        c(r, e)
      ] : [
        ...t,
        [c(r, e), "=", c(n, e)].join("")
      ];
  }
}
function T(e) {
  let r;
  switch (e.arrayFormat) {
    case "index":
      return (t, n, i) => {
        if (r = /\[(\d*)]$/.exec(t), t = t.replace(/\[\d*]$/, ""), !r) {
          i[t] = n;
          return;
        }
        i[t] === void 0 && (i[t] = {}), i[t][r[1]] = n;
      };
    case "bracket":
      return (t, n, i) => {
        if (r = /(\[])$/.exec(t), t = t.replace(/\[]$/, ""), !r) {
          i[t] = n;
          return;
        }
        if (i[t] === void 0) {
          i[t] = [n];
          return;
        }
        i[t] = [...i[t], n];
      };
    case "colon-list-separator":
      return (t, n, i) => {
        if (r = /(:list)$/.exec(t), t = t.replace(/:list$/, ""), !r) {
          i[t] = n;
          return;
        }
        if (i[t] === void 0) {
          i[t] = [n];
          return;
        }
        i[t] = [...i[t], n];
      };
    case "comma":
    case "separator":
      return (t, n, i) => {
        const a = typeof n == "string" && n.includes(e.arrayFormatSeparator), s = typeof n == "string" && !a && o(n, e).includes(e.arrayFormatSeparator);
        n = s ? o(n, e) : n;
        const f = a || s ? n.split(e.arrayFormatSeparator).map((l) => o(l, e)) : n === null ? n : o(n, e);
        i[t] = f;
      };
    case "bracket-separator":
      return (t, n, i) => {
        const a = /(\[])$/.test(t);
        if (t = t.replace(/\[]$/, ""), !a) {
          i[t] = n && o(n, e);
          return;
        }
        const s = n === null ? [] : n.split(e.arrayFormatSeparator).map((f) => o(f, e));
        if (i[t] === void 0) {
          i[t] = s;
          return;
        }
        i[t] = [...i[t], ...s];
      };
    default:
      return (t, n, i) => {
        if (i[t] === void 0) {
          i[t] = n;
          return;
        }
        i[t] = [...[i[t]].flat(), n];
      };
  }
}
function M(e) {
  if (typeof e != "string" || e.length !== 1)
    throw new TypeError("arrayFormatSeparator must be single character string");
}
function c(e, r) {
  return r.encode ? r.strict ? L(e) : encodeURIComponent(e) : e;
}
function o(e, r) {
  return r.decode ? R(e) : e;
}
function O(e) {
  return Array.isArray(e) ? e.sort() : typeof e == "object" ? O(Object.keys(e)).sort((r, t) => Number(r) - Number(t)).map((r) => e[r]) : e;
}
function x(e) {
  const r = e.indexOf("#");
  return r !== -1 && (e = e.slice(0, r)), e;
}
function V(e) {
  let r = "";
  const t = e.indexOf("#");
  return t !== -1 && (r = e.slice(t)), r;
}
function S(e, r) {
  return r.parseNumbers && !Number.isNaN(Number(e)) && typeof e == "string" && e.trim() !== "" ? e = Number(e) : r.parseBooleans && e !== null && (e.toLowerCase() === "true" || e.toLowerCase() === "false") && (e = e.toLowerCase() === "true"), e;
}
function y(e) {
  e = x(e);
  const r = e.indexOf("?");
  return r === -1 ? "" : e.slice(r + 1);
}
function F(e, r) {
  r = {
    decode: !0,
    sort: !0,
    arrayFormat: "none",
    arrayFormatSeparator: ",",
    parseNumbers: !1,
    parseBooleans: !1,
    ...r
  }, M(r.arrayFormatSeparator);
  const t = T(r), n = /* @__PURE__ */ Object.create(null);
  if (typeof e != "string" || (e = e.trim().replace(/^[?#&]/, ""), !e))
    return n;
  for (const i of e.split("&")) {
    if (i === "")
      continue;
    const a = r.decode ? i.replaceAll("+", " ") : i;
    let [s, f] = E(a, "=");
    s === void 0 && (s = a), f = f === void 0 ? null : ["comma", "separator", "bracket-separator"].includes(r.arrayFormat) ? f : o(f, r), t(o(s, r), f, n);
  }
  for (const [i, a] of Object.entries(n))
    if (typeof a == "object" && a !== null)
      for (const [s, f] of Object.entries(a))
        a[s] = S(f, r);
    else
      n[i] = S(a, r);
  return r.sort === !1 ? n : (r.sort === !0 ? Object.keys(n).sort() : Object.keys(n).sort(r.sort)).reduce((i, a) => {
    const s = n[a];
    return i[a] = s && typeof s == "object" && !Array.isArray(s) ? O(s) : s, i;
  }, /* @__PURE__ */ Object.create(null));
}
function $(e, r) {
  if (!e)
    return "";
  r = {
    encode: !0,
    strict: !0,
    arrayFormat: "none",
    arrayFormatSeparator: ",",
    ...r
  }, M(r.arrayFormatSeparator);
  const t = (s) => r.skipNull && H(e[s]) || r.skipEmptyString && e[s] === "", n = P(r), i = {};
  for (const [s, f] of Object.entries(e))
    t(s) || (i[s] = f);
  const a = Object.keys(i);
  return r.sort !== !1 && a.sort(r.sort), a.map((s) => {
    const f = e[s];
    return f === void 0 ? "" : f === null ? c(s, r) : Array.isArray(f) ? f.length === 0 && r.arrayFormat === "bracket-separator" ? c(s, r) + "[]" : f.reduce(n(s), []).join("&") : c(s, r) + "=" + c(f, r);
  }).filter((s) => s.length > 0).join("&");
}
function A(e, r) {
  var i;
  r = {
    decode: !0,
    ...r
  };
  let [t, n] = E(e, "#");
  return t === void 0 && (t = e), {
    url: ((i = t == null ? void 0 : t.split("?")) == null ? void 0 : i[0]) ?? "",
    query: F(y(e), r),
    ...r && r.parseFragmentIdentifier && n ? { fragmentIdentifier: o(n, r) } : {}
  };
}
function j(e, r) {
  r = {
    encode: !0,
    strict: !0,
    [g]: !0,
    ...r
  };
  const t = x(e.url).split("?")[0] || "", n = y(e.url), i = {
    ...F(n, { sort: !1 }),
    ...e.query
  };
  let a = $(i, r);
  a && (a = `?${a}`);
  let s = V(e.url);
  if (typeof e.fragmentIdentifier == "string") {
    const f = new URL(t);
    f.hash = e.fragmentIdentifier, s = r[g] ? f.hash : `#${e.fragmentIdentifier}`;
  }
  return `${t}${a}${s}`;
}
function C(e, r, t) {
  t = {
    parseFragmentIdentifier: !0,
    [g]: !1,
    ...t
  };
  const { url: n, query: i, fragmentIdentifier: a } = A(e, t);
  return j({
    url: n,
    query: D(i, r),
    fragmentIdentifier: a
  }, t);
}
function W(e, r, t) {
  const n = Array.isArray(r) ? (i) => !r.includes(i) : (i, a) => !r(i, a);
  return C(e, n, t);
}
const K = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  exclude: W,
  extract: y,
  parse: F,
  parseUrl: A,
  pick: C,
  stringify: $,
  stringifyUrl: j
}, Symbol.toStringTag, { value: "Module" })), u = "https://docmee.cn", m = {
  dashboard: `${u}/sdk-ui/dashboard`,
  editor: `${u}/sdk-ui/editor`,
  creator: `${u}/sdk-ui/creator/0`,
  customTemplate: `${u}/sdk-ui/custom-template`,
  templateCreator: `${u}/sdk-ui/custom-template-creator`,
  templateMarker: `${u}/sdk-ui/marker`
};
class B {
  /**
   *
   * @param {DocmeeUIConstructorOptions} options
   */
  constructor({
    token: r,
    page: t = "dashboard",
    container: n,
    pptId: i,
    onMessage: a,
    ...s
  }) {
    d(this, "docmeeHref", m.dashboard);
    d(this, "query", { iframe: "1" });
    d(this, "iframe", null);
    d(this, "onMessage", () => Promise.resolve(!0));
    d(this, "iframeMounted", !1);
    d(this, "initInterval", null);
    if (this.onMessage = a, this.container = n, location.protocol.startsWith("file") && console.log(
      "%c %s",
      "color: red; background-color: #f7c600",
      "🔴 不能在file协议下运行，请启动一个http服务来运行！ 🔴 "
    ), !r)
      return console.log(
        "%c 初始化时，token不能为空！",
        "color: #d7514f; background-color: #2e2e2e"
      );
    this.init({ token: r, page: t, pptId: i, ...s });
  }
  _postMessage(r) {
    var t, n;
    if (!this.iframe.contentWindow) throw new Error("iframe未挂载！");
    (n = (t = this.iframe) == null ? void 0 : t.contentWindow) == null || n.postMessage(r, this.docmeeHref);
  }
  init({ token: r, page: t = "dashboard", ...n }) {
    if (t === "editor" && !n.pptId)
      throw new Error("初始化editor页面时，必须传入pptId");
    this.query = Object.assign({}, this.query, n), this.docmeeHref = m[t], this.updateToken(r), this._initIframe(!0);
  }
  // 初始化iframe
  _initIframe(r) {
    const t = this.container, n = document.createElement("iframe"), i = location.href, a = K.stringifyUrl({
      url: this.docmeeHref,
      query: r ? { iframe: 1, targetOrigin: i } : this.query
    });
    n.src = a, n.style.width = "100%", n.style.height = "100%", n.style.border = "0", n.style.outline = "none", n.style.padding = "0px", n.setAttribute("allowfullscreen", "true"), this.iframe = n, this.iframeMounted = !1, t.innerHTML = "", t.appendChild(n), this.iframe.addEventListener("load", () => {
      setTimeout(() => {
        this.iframeMounted || (this.initInterval = setInterval(() => {
          if (this.iframeMounted)
            return clearInterval(this.initInterval);
          r && this._postMessage({
            type: "transParams",
            data: this.query
          });
        }, 200));
      }, 300), window.addEventListener("message", async (s) => {
        var l, p;
        if (s.source !== this.iframe.contentWindow) return;
        const f = s.data;
        if (r && f.type === "mounted" && (this.iframeMounted = !0, this._postMessage({
          type: "transParams",
          data: this.query
        })), f.type === "user-info" && (this.iframeMounted = !0), f.type.startsWith("before")) {
          const U = await ((l = this.onMessage) == null ? void 0 : l.call(this, f));
          this._postMessage({ data: U, type: `recover_${f.type}` });
        } else
          (p = this.onMessage) == null || p.call(this, f);
      });
    });
  }
  /**
   * 更新用户token
   * @param {string} latestToken 新的token
   */
  updateToken(r) {
    if (!/(a|s)k_.+/.test(r)) throw new Error("token 错误！");
    this.token = r, this.query.token = r, this.iframeMounted && this._postMessage({
      type: "transParams",
      data: {
        token: r
      }
    });
  }
  /**
   * 卸载iframe
   */
  destroy() {
    this.container.innerHTML = "";
  }
  /**
   * 发送消息
   * @param {{type: 'warning' | 'success' | 'error' | 'info', content: string}}
   */
  sendMessage(r) {
    this._postMessage({ type: "message", data: r });
  }
  getInfo() {
    this._postMessage({ type: "getInfo" });
  }
  navigate({ page: r, pptId: t, templateId: n }) {
    if (!m[r]) throw new Error(`页面${r} 不存在`);
    this._postMessage({
      type: "nav",
      data: {
        page: r,
        token: this.token,
        pptId: t,
        templateId: n
      }
    });
  }
}
window.DocmeeUI = B;
